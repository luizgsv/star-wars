import { KeyValues } from "@src/Models/Interfaces/keyValues";
/**
 * Objeto objectSubTitle
 * 
 * Este objeto mapeia chaves (keys) a valores (values) para fornecer subtítulos correspondentes a tipos de objetos.
 * 
 * @type {KeyValues}
 */
export const objectSubTitle: KeyValues = {
  ['character']: 'Personagens',
  ['person']: 'Características de Personagem'
}